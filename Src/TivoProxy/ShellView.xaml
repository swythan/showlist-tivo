<Window x:Class="TivoProxy.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*" />
            <ColumnDefinition
                Width="*" />
        </Grid.ColumnDefinitions>
        <DockPanel
            Grid.ColumnSpan="2">
            <Label
                Content="Real TiVo IP Address:"
                Grid.Column="0" />
            <TextBox
                x:Name="TivoIPAddress"
                VerticalAlignment="Center"
                Grid.Column="1" />
        </DockPanel>

        <Button
            x:Name="Start"
            Content="Start"
            Grid.Row="1"
            Margin="5" />
        <Button
            x:Name="Stop"
            Content="Stop"
            Grid.Column="1"
            Grid.Row="1"
            Margin="5" />
    </Grid>

</Window>